# v0.88

Исправления:
- HTML реально загружает app.js?v=088 (раньше оставался v=075, поэтому браузер мог брать старый JS из кэша).
- Кнопка ▶ запуска симуляции и кнопка ↺ сброса снова нормально размещаются на iPhone.
- Текст статуса больше не сжимается в узкую вертикальную колонку.
- Кнопка верхнего сброса теперь привязана к reset() внутри области симуляции и реально работает.
- Схема GPX в «Трек гонки → Анализ карты» на телефоне выводится отдельным полноширинным canvas под списком бродов.
- После анализа схема принудительно перерисовывается несколько раз, чтобы iOS успел рассчитать размеры canvas.
- Карта в самой симуляции остаётся убранной.
