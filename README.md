# v0.89

Исправлено:
- Картинка/сцена симуляции больше не исчезает, даже если прогноз ещё не рассчитан.
- После загрузки GPX схема маршрута рисуется сразу, без ожидания OSM-анализа.
- После 20 секунд анализа теперь только предупреждение, анализ НЕ обрывается.
- Жёсткий аварийный лимит увеличен до 45 секунд.
- Если даже 45 секунд не хватило, GPX-схема всё равно остаётся видимой.
- Убрана старая обработка TIMEOUT_20S.
