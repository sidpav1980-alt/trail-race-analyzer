# Trail Race Analyzer v0.93

- Раздел «Анализ карты» возвращён к интерфейсу v0.87.
- Убрана добавленная позже большая «Схема трека и броды» из результатов анализа.
- Возвращены метрики покрытия, список бродов, полоса покрытия и легенда как в v0.87.
- Симуляция и маленький человечек из текущей версии сохранены.
- Обновлён ключ кэша для Chrome.

# v0.91 — Chrome cache fix

- Исправлено зависание старой версии в Chrome/Chrome PWA.
- Service Worker теперь вызывает skipWaiting() и clients.claim().
- Старые cache storage удаляются при активации новой версии.
- HTML, JS, CSS и manifest загружаются по стратегии network-first с cache:no-store.
- При каждом открытии страницы Chrome принудительно проверяет обновление Service Worker.
- Offline-кэш картинок и статических файлов сохранён.
