# Trail Race Analyzer v0.26 OFFLINE

## v0.26
- Полностью пересобран механизм цветов кнопок.
- Исправлен раздел «Тренировки»: восстановлена отдельная кнопка загрузки.
- Во всех трёх разделах одна логика:
  - серый — файл не выбран;
  - синий — файл выбран;
  - серый — идёт обработка;
  - зелёный — успешно;
  - красный — ошибка.
- Цвет после успеха/ошибки больше не сбрасывается после `disabled=false`.
- Обновлён PWA cache до v0.26.

## Render
Static Site
Build Command: `echo OK`
Publish Directory: `.`
